{% extends 'base.html.twig' %}

{% block title %}Editar Historial Académico{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Editar Historial Académico</h1>

        {{ include('academic_history/_form.html.twig', {'button_label': 'Actualizar'}) }}

        <div class="mt-3">
            <a href="{{ path('app_academic_history_index') }}" class="btn btn-secondary">Volver al listado</a>
            {{ include('academic_history/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
