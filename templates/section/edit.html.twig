{% extends 'base.html.twig' %}

{% block title %}Editar Sección{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Editar Sección</h1>

        {{ include('section/_form.html.twig', {'button_label': 'Actualizar'}) }}

        <div class="mt-3">
            <a href="{{ path('app_section_index') }}" class="btn btn-secondary">Volver al listado</a>
            {{ include('section/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
