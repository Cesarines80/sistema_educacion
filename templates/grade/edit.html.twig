{% extends 'base.html.twig' %}

{% block title %}Editar Calificación{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Editar Calificación</h1>

        {{ include('grade/_form.html.twig', {'button_label': 'Actualizar'}) }}

        <div class="mt-3">
            <a href="{{ path('app_grade_index') }}" class="btn btn-secondary">Volver al listado</a>
            {{ include('grade/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
