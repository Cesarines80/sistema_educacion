{{ form_start(form) }}
    <div class="row">
        <div class="col-md-4">
            {{ form_row(form.value) }}
        </div>
        <div class="col-md-4">
            {{ form_row(form.student) }}
        </div>
        <div class="col-md-4">
            {{ form_row(form.section) }}
        </div>
    </div>
    <div class="mt-3">
        <button class="btn btn-primary">{{ button_label|default('Guardar1') }}</button>
    </div>
{{ form_end(form) }}
