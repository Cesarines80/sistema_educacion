{% extends 'base.html.twig' %}

{% block title %}Editar Semestre{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Editar Semestre</h1>

        {{ include('semester/_form.html.twig', {'button_label': 'Actualizar'}) }}

        <div class="mt-3">
            <a href="{{ path('app_semester_index') }}" class="btn btn-secondary">Volver al listado</a>
            {{ include('semester/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
