{% extends 'base.html.twig' %}

{% block title %}Editar Profesor{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Editar Profesor</h1>

        {{ include('teacher/_form.html.twig', {'button_label': 'Actualizar'}) }}

        <div class="mt-3">
            <a href="{{ path('app_teacher_index') }}" class="btn btn-secondary">Volver al listado</a>
            {{ include('teacher/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
