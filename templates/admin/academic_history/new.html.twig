{% extends 'admin/base_admin.html.twig' %}

{% block title %}Crear Nuevo Registro de Historial Académico{% endblock %}

{% block breadcrumb %}Crear Nuevo Registro{% endblock %}

{% block content %}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Crear Nuevo Registro de Historial Académico</h3>
                <div class="card-tools">
                    <a href="{{ path('app_academic_history_index') }}" class="btn btn-secondary btn-sm">
                        <i class="fas fa-arrow-left"></i> Volver a la Lista
                    </a>
                </div>
            </div>
            <div class="card-body">
                {{ include('admin/academic_history/_form.html.twig', {'button_label': 'Crear'}) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
