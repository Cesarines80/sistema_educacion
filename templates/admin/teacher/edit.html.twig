{% extends 'admin/base_admin.html.twig' %}

{% block title %}Editar Profesor{% endblock %}

{% block breadcrumb %}Editar Profesor{% endblock %}

{% block content %}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Editar Profesor</h3>
                <div class="card-tools">
                    <a href="{{ path('app_teacher_index') }}" class="btn btn-secondary btn-sm">
                        <i class="fas fa-arrow-left"></i> Volver a la Lista
                    </a>
                    {{ include('admin/teacher/_delete_form.html.twig') }}
                </div>
            </div>
            <div class="card-body">
                {{ include('admin/teacher/_form.html.twig', {'button_label': 'Actualizar'}) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
