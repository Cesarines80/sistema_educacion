{% extends 'base.html.twig' %}

{% block title %}Editar Materia{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Editar Materia</h1>

        {{ include('subject/_form.html.twig', {'button_label': 'Actualizar'}) }}

        <div class="mt-3">
            <a href="{{ path('app_subject_index') }}" class="btn btn-secondary">Volver al listado</a>
            {{ include('subject/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
